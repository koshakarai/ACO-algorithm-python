#!/usr/bin/env python
# -*- coding: utf-8 -*-
from matplotlib import pyplot as plt, animation
import networkx as nx

def draw():

def animate(GenFunc, PostFunc):

    A = animation.FuncAnimation(
        fig, 
        drawGraph, 
        frames = func,
        fargs=(fig, gs),
        interval=2, 
        repeat=False)

    plt.show()

